import 'package:canton_design_system/canton_design_system.dart';

class PreventionViewHeader extends StatelessWidget {
  const PreventionViewHeader();

  @override
  Widget build(BuildContext context) {
    return ViewHeaderTwo(
      title: 'Prevention',
      backButton: true,
    );
  }
}
