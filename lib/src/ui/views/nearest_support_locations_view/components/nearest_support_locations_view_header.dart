import 'package:canton_design_system/canton_design_system.dart';

class NearestSupportLocationsViewHeader extends StatelessWidget {
  const NearestSupportLocationsViewHeader();

  @override
  Widget build(BuildContext context) {
    return ViewHeaderTwo(
      title: 'Nearest Support Locations',
      backButton: true,
    );
  }
}
